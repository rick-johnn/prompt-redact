# spaCy transformer model, HASH-PINNED (threat T8 — closes the model-wheel
# integrity gap). Installed in the sidecar image with `--require-hashes --no-deps`
# (its dependencies are already provided, hash-pinned, by requirements.txt).
#
# Air-gapped builders: mirror this wheel inside your trust boundary and replace
# the URL with the mirror — the hash still verifies the artifact is identical.
en_core_web_trf @ https://github.com/explosion/spacy-models/releases/download/en_core_web_trf-3.8.0/en_core_web_trf-3.8.0-py3-none-any.whl \
    --hash=sha256:272a31e9d8530d1e075351d30a462d7e80e31da23574f1b274e200f3fff35bf5
